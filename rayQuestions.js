// 1.  A written set of instructions that performs a task and returns an output given a specific input.

// 2. A function always returns a value.

// 3. A return statement ends the function and sends a value back to the caller.

function addNum(x, y) {
	return (x + y);
}